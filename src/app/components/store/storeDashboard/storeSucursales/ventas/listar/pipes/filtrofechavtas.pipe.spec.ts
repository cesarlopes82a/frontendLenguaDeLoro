import { FiltrofechavtasPipe } from './filtrofechavtas.pipe';

describe('FiltrofechavtasPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrofechavtasPipe();
    expect(pipe).toBeTruthy();
  });
});
